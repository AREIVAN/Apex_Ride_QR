import type { EmergencyProfile } from '@/lib/emergency/types';
export function EmergencyHeader({ profile }: { profile: EmergencyProfile }) { const who = profile.alias || profile.name || 'SIN DATOS'; const sub = [profile.phone, profile.bike, profile.plate].filter(Boolean).join(' · '); return <div className="rounded-[2rem] border border-apex-danger/35 bg-apex-danger/15 p-6"><p className="text-xs font-black uppercase tracking-[.2em] text-red-200">Emergencia · prioridad alta</p><h1 className="mt-3 break-words text-4xl font-black tracking-[-.05em] sm:text-6xl">{who}</h1><p className="mt-2 text-lg text-red-100/80">{sub || 'Información limitada disponible'}</p></div>; }
