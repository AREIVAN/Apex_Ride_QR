import { Card } from '@/components/shared/Card';
export function VCardPreview({ vcard, link }: { vcard: string; link: string }) { return <Card className="p-5"><h3 className="text-sm font-black uppercase tracking-[.16em] text-apex-cyan">Payload técnico</h3><div className="mt-4 grid gap-3 text-xs"><div><b className="text-apex-muted">Link online</b><p className="mt-1 break-all rounded-2xl bg-black/25 p-3 font-mono text-apex-muted">{link || '—'}</p></div><div><b className="text-apex-muted">vCard offline</b><pre className="mt-1 max-h-48 overflow-auto whitespace-pre-wrap rounded-2xl bg-black/25 p-3 font-mono text-apex-muted">{vcard || '—'}</pre></div></div></Card>; }
