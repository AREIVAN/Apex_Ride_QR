import { ShieldCheck, Menu } from 'lucide-react';
import Link from 'next/link';
import { navigation } from '@/lib/constants/navigation';
import { Button } from '@/components/shared/Button';
export function SiteHeader() { return <header className="no-print sticky top-0 z-50 border-b border-white/10 bg-apex-void/72 backdrop-blur-xl"><div className="apex-container flex h-20 items-center justify-between gap-4"><Link href="/" className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-gradient-to-br from-apex-violet to-apex-cyan shadow-glow"><ShieldCheck className="size-5" /></span><span><strong className="block text-sm font-black sm:text-base">APEX Emergency QR</strong><small className="hidden text-xs text-apex-muted sm:block">Rider emergency identity</small></span></Link><nav className="hidden items-center gap-2 md:flex">{navigation.map((item) => <Link key={item.href} href={item.href} className="rounded-full px-4 py-2 text-sm font-bold text-apex-muted transition hover:bg-white/10 hover:text-white">{item.label}</Link>)}<Button href="/crear" className="px-4 py-2">Crear QR</Button></nav><Menu className="size-6 text-apex-muted md:hidden" /></div></header>; }
